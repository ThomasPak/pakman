# Epsilon
2,1,0

# Simulator
./persistent-mpi-simulator 1 @return_code@

# Parameter names
@parameter_name@

# Prior sampler
echo @sampled_parameter@

# Perturber
bash -c 'cat >/dev/null; echo 1'

# Prior pdf
bash -c 'cat >/dev/null; echo 1'

# Perturbation pdf
bash -c 'read t; \
         read new_p; \
         cat'
